<% flash.each do |name, msg| %>
  <% if msg.is_a?(String) %>
    <div class="alert alert-dismissible alert-<%= get_flash_class(name) %>">
      <a class="close" data-dismiss="alert">&#215;</a>
      <%= msg %>
    </div>
  <% end %>
<% end %>